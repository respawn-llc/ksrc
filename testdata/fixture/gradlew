#!/bin/sh
# Fake gradle wrapper for tests.
for arg in "$@"; do
  if [ "$arg" = "ksrcSources" ]; then
    echo "KSRCDEP|org.jetbrains.kotlinx:kotlinx-datetime:0.7.1"
    if [ -n "$KSRC_TEST_JAR" ]; then
      echo "KSRC|org.jetbrains.kotlinx:kotlinx-datetime:0.7.1|$KSRC_TEST_JAR"
    fi
  fi
done
exit 0
